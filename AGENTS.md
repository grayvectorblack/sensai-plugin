# Coding Agent Rules

- Keep this repository public-safe: never add server internals, private methodologies, secrets,
  credentials, or local machine paths.
- Specify the public MCP contract and plugin behavior before implementing packages.
- Keep tests and build scripts outside generated install payloads.
- Do not create platform packages until their specifications and red tests are accepted.
- Report every validation command run and any validation that could not be run.
